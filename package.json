{
  "name": "weatherapp",
  "version": "1.0.0",
  "description": "",
  "main": "electron/main.js",
  "type": "commonjs",
"scripts": {
  "dev:react": "vite --port 5199",
  "dev:electron": "wait-on http://localhost:5199 && electron . --watch electron",
  "dev": "concurrently -k \"npm run dev:react\" \"npm run dev:electron\"",
  "build:react": "vite build",
  "build": "npm run build:react"
},
  "dependencies": {
    "electron": "^31.0.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.0.0",
    "concurrently": "^8.2.0",
    "vite": "^5.0.0",
    "wait-on": "^7.0.1"
  }
}
